{
  "version": 2,
  "name": "nimfadora-tardis",
  "cleanUrls": true,
  "builds": [
    {
      "src": "vercel/*.go",
      "use": "@vercel/go",
      "config": {
        "includeFiles": ["vercel/dashboard.html"]
      }
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/vercel" },
    { "source": "/mcp", "destination": "/vercel" },
    { "source": "/(.*)", "destination": "/vercel" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" }
      ]
    }
  ]
}
